"""tools for interpolation and discretization"""
#!/usr/bin/env python3
def interpolate(pot_values, points, type):
    """interpolates given potential
    input:
        pot_values: potential values in x-V(x) pairs
        points: number of potential values
        type: type of interpolation
    return:
        values of interpolated potential or false if failure
    """
    return pot_values
def discretize(pot_values):
    """discretizes given function"""
    return pot_values
